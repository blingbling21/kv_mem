[package]
name = "kv_mem"
version = "0.1.0"
edition = "2024"

[dependencies]
storage = { path = "../storage" }
protocol = { path = "../protocol" }
thiserror = "2.0.18"
tokio = { version = "1", features = ["full"] }
